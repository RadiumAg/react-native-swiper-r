import { StyleSheet } from 'react-native';

export const Style = StyleSheet.create({
    contentContainerStyle: {
        alignItems: 'center',
    },

    childContainerStyle: {
        flexDirection: 'row',
    },
});
